name: goraiya
description: A new Flutter project.
version: 0.1.0

environment:
  sdk: ">=3.0.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  appwrite: ^8.1.0
  flutter_svg: ^2.0.7
  google_fonts: ^4.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
flutter:
  uses-material-design: true
  assets:
    - assets/svgs/
    - assets/pngs/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"
